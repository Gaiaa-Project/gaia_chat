KeybindsConfig = {
    --- Toggle key
    ---
    --- The key used to open the chat input.
    --- Uses FiveM key mapping names.
    --- The player can rebind this key in their GTA settings.
    ---
    --- Default: 'T'
    toggleKey = 'T',

    --- Passive mode toggle key
    ---
    --- The key used to toggle the passive chat mode (dynamic / hidden).
    --- Uses FiveM key mapping names.
    --- The player can rebind this key in their GTA settings.
    ---
    --- Default: 'L'
    passiveToggleKey = 'L',
}
