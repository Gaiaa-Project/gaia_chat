HistoryConfig = {
    --- Command history size
    ---
    --- The maximum number of executed commands saved in history.
    --- Navigate through history with arrow up/down keys in the chat input.
    --- Persisted between sessions via KVP.
    ---
    --- Set to 0 to disable command history.
    ---
    --- Default: 20
    maxCommands = 20,
}
