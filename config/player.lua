PlayerConfig = {
    --- OOC (Out Of Character) messages
    ---
    --- Allows players to send out-of-character messages via /ooc.
    --- Useful for non-roleplay communications (technical questions, etc.).
    ---
    --- true: Players can send and see OOC messages.
    --- false: OOC channel is fully disabled, /ooc command is inaccessible.
    ---
    --- Default: true
    oocEnabled = true,
}
